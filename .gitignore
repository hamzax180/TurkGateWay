# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Local env files with secrets — never commit these
.env.local
.env*.local

# Visa applicant PII — never commit this, it holds passport-level data
scripts/visa-booking-assistant/applicant.json

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# python backend
venv/
__pycache__/
.env

# Custom folders
/logs/
/data/

# Logs and DBs
*.log
*.db
*.sqlite3
db.sqlite3
learning_log.json
response_dump.json

# Desktop and Build artifacts
/dist-desktop/
/desktop/node_modules/
/desktop/dist/
/public/releases/
/src/app/download/
*.exe
*.node
*.sst

# Agent folders
.claude/

# Database exports taken before destructive migrations — may contain user data.
backups/

# Outreach send history — a contact record, not source.
scripts/university-outreach/sent_log.json

# İkamet assistant applicant data — passport-level PII, never commit.
scripts/ikamet-assistant/applicant.json
